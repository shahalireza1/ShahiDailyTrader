symbols:
  - SPY
  - QQQ
  - IWM
start: "2018-01-01"
end: "2024-01-01"
mode: backtest
fees_bps: 1.0
slippage_bps: 1.0
starting_cash: 200000
strategies:
  - name: momentum
    parameters:
      lookback: 120
      threshold: 0.01
  - name: sma_cross
    parameters:
      fast: 30
      slow: 120
  - name: buy_hold
    parameters: {}
ensemble:
  mode: risk_budget
  vol_lookback: 60
  min_weight: 0.12
risk:
  max_drawdown: 0.22
  max_drawdown_stop: 0.15
  drawdown_safe_fraction: 0.4
  max_gross_exposure: 0.6
  max_position_per_symbol: 0.25
  vol_target_annual: 0.13
  vol_lookback: 60
  trade_cooldown_days: 1
  position_mode: fixed_fraction
  position_fraction: 0.95
output_dir: outputs
